<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <title>Login</title>
    <style>
        body {
            background-image: url("../image/banner1.jpg");
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
            background-position: center;
        }

        .container {
            margin-top: 50px;
        }

        .card {
            background-color: #ffffff2c;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 550px;
            text-align: center;
        }

        .card-header {
            font-size: 20px;
            font-weight: bold;
            text-align: center;
            border-radius: 15px 15px 0 0;
        }

        .card-body {
            padding: 20px;
            color: white;
        }

        .form-label {
            font-weight: bold;
            color: rgb(255, 255, 255);
        }

        .form-control {
            border: none;
            border-bottom: 2px solid white;
            /* Add a bottom border */
            background-color: transparent;
            /* Make the background transparent */
            color: rgb(255, 255, 255);
        }

        .btn-primary {
            width: 100%;
            margin-bottom: 10px;
            border: 1px solid #af0957;
            /* Pink border */
            background-color: #af0957;
            /* Pink background color */
            color: white;
        }

        .btn-signup {
            width: 100%;
            border: 1px solid #af0957;
            /* Pink border */
            background-color: #af0957;
            /* Pink background color */
            color: white;
        }

        .btn-secondary {
            background-color: #6c757d;
            /* Change to a different color if needed */
            color: white;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="mt-3 mb-3 text-center text-primary"><b>Chào mừng tới Hust Airline</b></h1>
        <h3 class="mb-3 mt-3 text-center text-primary"><b>Đăng nhập để tiếp tục</b></h3>
        <div class="row">
            <div class="col-sm-3"></div>
            <div class="col-sm-6 mb-3 mt-3">
                <div class="card">
                    <div class="card-header">
                        <% if(message.success) { %>
                            <div class="alert alert-success">
                                <%= message.success %>
                            </div>
                            <% } %>

                                <% if(message.fail) { %>
                                    <div class="alert alert-danger">
                                        <%= message.fail %>
                                    </div>
                                    <% } %>
                    </div>
                    <div class="card-body">
                        <form method="post" action="/project/login">
                            <div class="mb-3">
                                <label for="user_name" class="form-label">Tên đăng nhập</label>
                                <input type="text" id="user_name" name="user_name" class="form-control" />
                            </div>
                            <div class="mb-3">
                                <label for="user_password" class="form-label">Mật Khẩu</label>
                                <input type="password" id="user_password" name="user_password" class="form-control" />
                            </div>
                            <div class="mb-3">
                                <input type="submit" class="btn btn-primary" value="Đăng nhập" />
                            </div>
                            <div class="mb-3">
                                <a href="/project/register" class="btn btn-primary btn-signup">Người dùng mới? Đăng
                                    ký</a>
                            </div>
                            <div class="mb-4">
                                <a href="/project/admin_cred" class="btn btn-primary">Đăng nhập với Admin</a>
                            </div>
                        </form>

                        <!-- Nút "Back to Homepage" -->
                        <button onclick="window.location.href='/'" class="btn btn-secondary">Quay về trang chủ</button>
                    </div>
                </div>
            </div>
            <div class="col-sm-3"></div>
        </div>
    </div>
</body>

</html>