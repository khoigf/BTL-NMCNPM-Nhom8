<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/styleUser.css' />
    <title>Admin Page</title>
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
            padding: 0;
        }

        #sidebar {
            width: 250px;
            background-color: #343a40;
            color: #ffffff;
            padding: 20px;
        }

        table {
            width: 100%;
            background-color: white;
            margin-top: 20px;
            border-collapse: collapse;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
        }

        th,
        td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #dee2e6;
        }

        tbody tr:hover {
            background-color: #f5f5f5;
        }

        .btn-login {
            background-color: #007bff;
            color: #ffffff;
        }

        .btn-login:hover {
            background-color: #0069d9;
            color: #ffffff;
        }
    </style>
</head>

<body>
    <div class="container mt-4">
        <h1>Danh sách khách hàng</h1>

        <table class="table mt-4">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Tên</th>
                    <th>Email</th>
                    <th>Số điện thoại</th>
                    <th>Địa chỉ</th>
                    <th></th>
                    <!-- Add more columns as needed -->
                </tr>
            </thead>
            <tbody>
                <% for (let user of users) { %>
                    <tr>
                        <td>
                            <%= user.user_id %>
                        </td>
                        <td>
                            <%= user.user_name %>
                        </td>
                        <td>
                            <%= user.user_email %>
                        </td>
                        <td>
                            <%= user.user_mobile %>
                        </td>
                        <td>
                            <%= user.user_address %>
                        </td>
                        <td>
                            <button class="btn btn-primary"
                                onclick="window.location.href='/project/admin_login/edit_user/<%= user.user_id %>'">Cập
                                nhật</button>
                        </td>
                        <!-- Add more columns as needed -->
                    </tr>
                    <% } %>
            </tbody>
        </table>

        <!-- Add User Form -->
        <div class="mt-4">
            <h1>Thêm khách hàng</h1>
            <button class="btn btn-primary" onclick="window.location.href='/project/admin_login/add_user'">Thêm khách
                hàng</button>
        </div>
    </div>
</body>

</html>